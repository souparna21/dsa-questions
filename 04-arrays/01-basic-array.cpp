#include <bits/stdc++.h>

using namespace std;

int main()
{
    // creating a fix sized array
    int arr1[100];               // Stack allocated memory

    int n=100;                   // Stack allocated memory
    int arr2[n];                 // Stack allocated memory

    int arr[] = {10,14,30,40};   // Stack allocated memory

    int *arr3 = new int[n];      // Heap allocated memory(dynamically allocated)
    // In java array's are always allocated in Heap

    // Crating dynamic sized array
    vector<int> v;

    return 0;
}